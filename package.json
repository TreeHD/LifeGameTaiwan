{
  "name": "taiwan-life-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev",
    "refresh-data": "cd backend && npm run refresh",
    "pages:dev": "wrangler pages dev frontend/dist --compatibility-date=2025-01-01"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
